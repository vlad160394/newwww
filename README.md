# newwwwint Sum(int N)
{
  int sum = 0;
  for (int i = 0; i < N; i++)
  {
    sum += i * i;
  }
  return sum;
}int Sum(int N)
{
  int sum = 0;
  for (int i = 0; i < N; i++)
  {
    sum += i * i;
  }
  return sum;
}
